
=== ZADANIE: CHUNK 4/10 — CSS część C (opinie + specyfikacja) ===
Sam CSS (bez <style>). Klasy:
- OPINIE: .rev-head (grid score+tekst), .rev-score (.n duża liczba, .stars --gold), .rev-basis, .rev-source,
  .rev-honest (karta wyróżniona, border-left 4px --cta lub --brand, tag „NAJUCZCIWSZA OPINIA”, blockquote, .who),
  .rev-grid (grid 5→3→2), .rev-tile (aspect 3/4, cover, zoom-in), .rev-cap (gradient dołu, .stars, strong),
  .rev-quotes (grid 2→1), .quote (karta), .quote .stars/.quote p/.quote strong.
- SPECYFIKACJA: .specs (grid np. 3-4 kol → 2 → 1), .spec-tile (karta biała, cień), .spec-ic (obrys --brand, tło cool/blush), .spec-tile b (liczba/nazwa), .spec-tile span (opis).
Uwaga: .ugc filter jak w galerii. JASNE tła. Output = SUROWY CSS bez ``` i bez komentarzy.
